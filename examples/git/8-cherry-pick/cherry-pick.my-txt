`git cherry-pick хешКоммита хешКоммита хешКоммита`
копирует указанные коммиты в текущую ветку

можно написать название ветки
`git cherry-pick НАЗВАНИЕ_ВЕТКИ`
тогда скопируются все коммиты которые достижимы из указанной ветки, но не достижимы из текущей ветки
