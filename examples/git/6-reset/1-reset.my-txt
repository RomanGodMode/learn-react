`git reset коммит`
позволяет перевесить ветку на другой коммит(коммит можно указать хешем коммита или веткой)

бывают разные виды ресетов(
 если в файлах есть незакомиченные изменения отслеживаемые гитом:
 hard -> они просто исчезнут
 soft -> останутся
)
