`git branch "feature/T1"`(создаём ветку)
`git checkout "feature/T1"`(переходим на ветку)
тоже самое можно сделать одной командой `git checkout -b "feature/T1"`

коммиты будут добавляться именно к той ветке на которой мы стоим

допустим мы создали ветку и закомитили в неё, но всё это есть у нас только локально
это надо запушить
`git push "feature/T1"`



git checkout позволяет не только переходить на ветки
но и на коммит по хешу коммита
`git checkout a2dc5efe`
